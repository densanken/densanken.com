---
import { Image } from "astro:assets";

import GitHubLogo from "../assets/GitHubLogo.svg";
import XLogo from "../assets/XLogo.svg";

const footerMenuItems = [
  { title: "トップ", href: "/" },
  { title: "News", href: "/news" },
  { title: "活動紹介", href: "/activities" },
  { title: "作品集", href: "/works" },
  { title: "お問い合わせ", href: "/contact" },
  { title: "プライバシーポリシー", href: "/privacy" },
] satisfies {
  title: string;
  href: string;
}[];

const year = new Date().getFullYear();
---

<footer class="pt-12 pb-6 text-center">
  <div class="flex flex-col items-center justify-evenly gap-12 md:flex-row md:items-start">
    <!-- PCメニュー -->
    <nav aria-label="フッターナビゲーション" class="hidden flex-col items-start gap-2 md:flex">
      {footerMenuItems.map((item) => <a href={item.href}>{item.title}</a>)}
    </nav>
    <nav aria-labelledby="footer-social-title footer-social-links" class="flex flex-col items-center gap-6">
      <p id="footer-social-title">千葉大学電子計算機研究会</p>
      <span id="footer-social-links" class="sr-only">各種リンク</span>
      <div class="flex flex-col gap-6">
        <a
          href="https://x.com/densanken"
          target="_blank"
          rel="noopener noreferrer"
          class="flex items-center gap-2"
          aria-label="X（旧Twitter）でんさんけん（新しいタブで開きます）"
        >
          <Image src={XLogo} alt="" width={28} height={28} />
          <span>@densanken</span>
        </a>
        <a
          href="https://github.com/densanken"
          target="_blank"
          rel="noopener noreferrer"
          class="flex items-center gap-2"
          aria-label="GitHub でんさんけん（新しいタブで開きます）"
        >
          <Image src={GitHubLogo} alt="" width={28} height={28} />
          <span>densanken</span>
        </a>
      </div>
    </nav>
    <!-- モバイルメニュー（見た目順とフォーカス順を一致させるため） -->
    <nav aria-label="フッターナビゲーション" class="flex flex-col items-start gap-2 md:hidden">
      {footerMenuItems.map((item) => <a href={item.href}>{item.title}</a>)}
    </nav>
  </div>
  <p class="mt-8">
    Copyright &copy; {year} 千葉大学電子計算機研究会<br class="md:hidden" /> All rights reserved.
  </p>
</footer>
