pre-commit:
  parallel: false
  commands:
    lint:
      glob: "*.{ts,tsx,astro}"
      run: pnpm eslint --fix {staged_files}
      stage_fixed: true
    format:
      run: pnpm prettier --write --ignore-unknown {staged_files}
      stage_fixed: true
